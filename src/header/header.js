angular.module('app.header', [])

  .controller('HeaderController',
    function ($scope) {

      // actions
      $scope.actions = {
        init: function () {
          // TODO
        }
      };
      $scope.actions.init();
    }
  );
